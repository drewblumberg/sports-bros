mixin field(colSize, type, name, text, value, placeholder, autofocus, id)
  div(class='#{colSize} columns')
    if text
      label(for=name) #{text}:
    input(id=id value=value, type=type, name=name, placeholder=placeholder, autofocus=autofocus)
